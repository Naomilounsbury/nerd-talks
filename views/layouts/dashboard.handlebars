<html lang="en">

  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>nerd</title>
    <!--Import Google Icon Font-->
    <link
      href="https://fonts.googleapis.com/icon?family=Material+Icons"
      rel="stylesheet"
    />
    <!--Import materialize.css-->
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css"
    />

    <!--Let browser know website is optimized for mobile-->
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  </head>

  <body>
    <div class="wrapper">
      <header>
        <nav>
          <div class="nav-wrapper purple accent-3">
            <a href="#" class="brand-logo">Nerd Talks</a>
            <ul id="nav-mobile" class="right white-text">
              <li><a href="/">Browse Posts</a></li>
              <li><a href="/dash">Your Dashboard</a></li>
            </ul>

          </div>
        </nav>
        {{#if loggedIn}}
          {{!-- {{log loggedIn}} --}}
          <button id="logout" class="btn purple darken-4 white-text">
            <a href="#">Logout</a></button>
        {{else}}
          <button id="login" class="btn purple darken-4 white-text">
            <a href="/login">Login</a></button>
        {{/if}}
      </header>
      <main>
        {{{body}}}
      </main>
      <footer>
        Thanks for visiting!
      </footer>
    </div>
    <script src="/js/logout.js"></script>
    <script
      src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js"
    ></script>
  </body>

</html>